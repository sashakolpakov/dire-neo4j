<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>org.dire</groupId>
    <artifactId>dire-neo4j-parent</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <packaging>pom</packaging>

    <name>dire-neo4j</name>
    <description>Neo4j-native DiRe graph layout plugin</description>

    <modules>
        <module>java-core</module>
        <module>neo4j-plugin</module>
        <module>benchmarks</module>
        <module>neo4j-benchmarks</module>
    </modules>

    <properties>
        <maven.compiler.release>17</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <neo4j.version>5.26.27</neo4j.version>
        <jaxrs.version>2.1.6</jaxrs.version>
        <junit.version>5.10.2</junit.version>
        <jmh.version>1.37</jmh.version>
        <excludedTestGroups>large</excludedTestGroups>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.junit.jupiter</groupId>
                <artifactId>junit-jupiter</artifactId>
                <version>${junit.version}</version>
                <scope>test</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>3.13.0</version>
                    <configuration>
                        <release>${maven.compiler.release}</release>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>3.2.5</version>
                    <configuration>
                        <useModulePath>false</useModulePath>
                        <excludedGroups>${excludedTestGroups}</excludedGroups>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

    <profiles>
        <profile>
            <id>neo4j-5.26</id>
            <activation>
                <activeByDefault>true</activeByDefault>
            </activation>
            <properties>
                <neo4j.version>5.26.27</neo4j.version>
            </properties>
        </profile>
        <profile>
            <id>neo4j-2026.05</id>
            <properties>
                <neo4j.version>2026.05.0</neo4j.version>
                <!-- Align the test engine with Neo4j 2026's JUnit Platform 6. -->
                <junit.version>6.0.3</junit.version>
            </properties>
        </profile>
        <profile>
            <id>large-tests</id>
            <properties>
                <excludedTestGroups>none</excludedTestGroups>
            </properties>
        </profile>
    </profiles>
</project>
