# Benchmarks

Planned benchmark area for synthetic graph fixtures and Neo4j-backed scale
runs. Keep benchmark code separate from `java-core` so the core layout engine
remains lightweight and testable without a database process.
